SELECT TRUNCATE((SALAR/1000), 0)*1000 AS DE, TRUNCATE((SALAR/1000), 0)*1000+999 A, COUNT(*) NUM_EMPLEADOS
FROM TEMPLE
GROUP BY TRUNCATE(SALAR/1000,0)
ORDER BY 1;
set @@global.sql_mode:=replace(@@global.sql_mode, 'ONLY_FULL_GROUP_BY', '');